import { OrdinalSufixPipe } from './ordinal-sufix.pipe';

describe('OrdinalSufixPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdinalSufixPipe();
    expect(pipe).toBeTruthy();
  });
});
