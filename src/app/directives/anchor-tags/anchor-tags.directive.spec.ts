import { AnchorTagsDirective } from './anchor-tags.directive';

describe('AnchorTagsDirective', () => {
  it('should create an instance', () => {
    const directive = new AnchorTagsDirective();
    expect(directive).toBeTruthy();
  });
});
